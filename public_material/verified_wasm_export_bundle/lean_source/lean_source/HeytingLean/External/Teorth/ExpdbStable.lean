/-
Teorth / expdb.

This wrapper exists so we can:
- build a stable import target under HeytingLean, and
- index `expdb.*` declarations into `lean_index/` via the multi-pass extractor.
-/

import expdb

