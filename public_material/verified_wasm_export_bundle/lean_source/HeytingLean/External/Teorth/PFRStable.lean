/-
Teorth / PFR (Polynomial Freiman-Ruzsa) project.

This wrapper exists so we can:
- build a stable import target under HeytingLean, and
- index `PFR.*` declarations into `lean_index/` via the multi-pass extractor.
-/

import PFR

