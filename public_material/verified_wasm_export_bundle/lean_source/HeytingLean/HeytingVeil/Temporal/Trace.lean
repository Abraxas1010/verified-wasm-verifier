import HeytingLean.HeytingVeil.Temporal.Core

namespace HeytingLean.HeytingVeil.Temporal

abbrev StatePred (α : Type u) := α → Prop
abbrev StepRel (α : Type u) := α → α → Prop

end HeytingLean.HeytingVeil.Temporal
